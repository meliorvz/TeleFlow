# TeleFlow Configuration
# Copy this file to localdata/config.env and fill in your values

# ==============================================================================
# TELEGRAM AUTHENTICATION (Required)
# ==============================================================================
# Get your API credentials from https://my.telegram.org
TG_API_ID=your_api_id_here
TG_API_HASH=your_api_hash_here

# ==============================================================================
# LLM PROVIDER (Optional - enables AI-powered reports)
# ==============================================================================
# Choose your provider: openrouter | venice
LLM_PROVIDER=openrouter

# OpenRouter (https://openrouter.ai)
# OPENROUTER_API_KEY=your_openrouter_key_here
# OPENROUTER_MODEL=deepseek/deepseek-v3.2

# Venice AI - privacy-focused (https://venice.ai)
# VENICE_API_KEY=your_venice_key_here
# VENICE_MODEL=deepseek-v3.2

# Custom system prompt for LLM (optional)
# LLM_SYSTEM_PROMPT=You are a helpful assistant...

# ==============================================================================
# SYNC SETTINGS
# ==============================================================================
# Auto-sync interval in minutes (0 to disable)
SYNC_INTERVAL_MINUTES=10

# Maximum participants to sync per group (prevents slowdown in large groups)
SYNC_MAX_PARTICIPANTS_PER_GROUP=100

# ==============================================================================
# REPORT GENERATION
# ==============================================================================
# Number of recent messages to analyze per conversation
REPORT_MESSAGE_LIMIT=20

# Max characters per message (0 = no truncation)
REPORT_TEXT_TRUNCATION=500

# Only include conversations active within this many days
LLM_CONVERSATION_MAX_AGE_DAYS=30

# Report schedule: manual | daily | weekly
REPORT_CADENCE=manual

# ==============================================================================
# BULK SEND
# ==============================================================================
# Maximum recipients per bulk send job
BULK_SEND_MAX_PER_JOB=200

# Delay between messages in seconds (rate limiting)
BULK_SEND_DELAY_SECONDS=10

# ==============================================================================
# SERVER
# ==============================================================================
WEB_HOST=127.0.0.1
WEB_PORT=8080

# ==============================================================================
# ADVANCED
# ==============================================================================
# Message cache limit per conversation
MESSAGE_CACHE_LIMIT=50

# Custom data directory (default: localdata/ within app folder)
# DATA_DIR=/path/to/custom/data
